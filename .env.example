# ===========================
# Prospector-API (.env.example)
# Base64-only secrets
# ===========================

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Google Maps / Places
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Google Sheets & Drive (Service Account)
# - Encode the ENTIRE service account JSON file to base64.
# - Used to create/append Google Sheets and (optionally) create them in a folder.
GOOGLE_SERVICE_ACCOUNT_EMAIL=service-account@your-google-project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_KEY_BASE64=BASE64_OF_FULL_GOOGLE_SA_JSON
# Optional: where to create Sheets
# GOOGLE_DRIVE_PARENT_FOLDER_ID=your_drive_folder_id

# Firebase Admin (Firestore)
# - Encode the ENTIRE Firebase Admin service account JSON file to base64.
# - Code will parse the JSON and use its private_key at runtime.
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@your-firebase-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY_BASE64=BASE64_OF_FULL_FIREBASE_SA_JSON

# Optional: completion webhook for downstream agents
# OUTBOX_WEBHOOK_URL=https://your-webhook.example.com

# Tuning
RATE_LIMIT_RPS=8
ENRICH_HOST_RPS=1
MAX_RESULTS=200
